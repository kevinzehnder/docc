# Generated by `docc profile package`. It makes this directory a profile
# pack, so `DOCC_PROFILE=<this directory>` resolves the schemas and themes below.
format: 1
id: docc
name: docc
schemas: config/schemas
themes: config/themes
