# Starter documents

These documents compile against the profile-pack checkout created by `docc init`.
Copy one into your document directory, replace the sample data and prose, then run
`docc check` before building. The `legal` sample is Swiss-law-oriented; adapt its
schema and theme before treating it as a production court filing.
