You are splitting one offer of proof from a Swiss legal brief into its
individual items. This is a text-editing task, not a writing task: every word of
the output must come from the input.

An offer of proof lists the evidence for the paragraph above it. It runs
together in the transcription because the original was laid out in columns, so
one item's description and the next item's label can end up on the same line, or
one item can be split across two lines.

Return one markdown list item per piece of evidence, in this shape:

    - [Label] description

The label goes in brackets and names the kind of proof. Use the label the
document itself uses, normalised to its usual spelling:

    - [Beilage 7] Schreiben der Beklagten an die Klägerin vom 20.09.2015
    - [Parteibefragung] Christian Magnani, Mitglied der Geschäftsleitung Klägerin
    - [Zeugnis] Alain Luchsinger
    - [Edition] Buchhaltungsunterlagen 2022–2024

Rules:

- One item per piece of evidence. A document and a witness are two items even
  when the transcription ran them onto one line.
- An attachment reference — "Beilage 7", "Klagebeilage 3", "Actorum 33" —
  usually comes *after* the document it belongs to, and it is that document's
  label, not evidence of its own. Move it to the front:

      Mietvertrag vom 03./07.09.2001 Beilage 3
      -> - [Beilage 3] Mietvertrag vom 03./07.09.2001

  Never emit a label with nothing after it. Every item needs a description.
- Where it names a form of proof instead — Parteibefragung, Beweisaussage,
  Zeugnis, Edition, Augenschein, Gutachten — that is the label, and the person
  or thing is the description.
- Where a line names both a document and a form of proof, split it into two.
- Preserve names, dates, reference numbers and special characters (ä ö ü ß «»)
  exactly as they appear. Do not translate, expand or tidy them.
- Invent nothing. If the input does not say what kind of proof an item is, use
  [Beweismittel] rather than guessing.

Return only the list. No heading, no fence, no commentary.

Here is the block:

{{ .Block }}
