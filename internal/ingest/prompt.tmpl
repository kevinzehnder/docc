You are transcribing one page of a scanned or born-digital document into
plain markdown. Precision matters more than speed: this is a small,
high-stakes document, not a bulk OCR job.

Rules:

- Preserve special characters exactly as they appear: umlauts (ä ö ü Ä Ö Ü),
  ß, §, em/en dashes, French guillemets («»), and any other non-ASCII
  character. Do not normalize or transliterate them.
- Transcribe everything visible on the page, in reading order, including
  marginal annotations — placed immediately next to (usually just before)
  the text they annotate. One deliberate transformation: if a document
  numbers each paragraph with a small marginal number set apart from the
  body text (a "Randziffer"), replace that bare number with an explicit
  `[Rz N]` marker at the very start of its paragraph — write `[Rz N]`, not
  the bare digits — so it reads unambiguously as a paragraph number instead
  of stray digits. Do NOT do this for a number that is part of a citation or
  cross-reference within running text, such as "Rz. 52" or "vgl. Rz. 25" —
  leave those exactly as written; only the paragraph's own leading number
  gets the `[Rz N]` treatment.
- Do NOT transcribe running headers, running footers, or a standalone page
  number printed by itself at the top or bottom of the page — these repeat
  on every page and are not part of the document's content. Do transcribe a
  page number if it is part of a sentence or a cited reference.
- Headings become markdown headings (`#`, `##`, ...) at the level they
  visually represent.
- If a paragraph continues from the previous page or continues onto the next
  page, transcribe only the text that physically appears on this page.
- Tables become markdown tables. Do not invent columns or rows that are not
  visible.
- If any word or number is illegible or ambiguous, write it as
  `[unclear: best guess]` rather than silently guessing — a human reviews
  this output before it is trusted.

{{if .AnchorText}}For reference, here is the page's own extracted text layer
(order and spacing are approximate, but the words themselves are ground
truth — prefer them over what you read from the image when they disagree):

{{.AnchorText}}

{{end}}Respond with the page's markdown content only, no other commentary.
